# AGENTS.md

## Cursor Cloud specific instructions

This repository (`qianfamily`) is currently an empty project with only a `README.md`. There are no application services, dependencies, or build systems configured yet.

- **No package manager lockfiles** exist — when code is added, install dependencies according to the lockfile type (e.g., `pnpm-lock.yaml` → pnpm, `package-lock.json` → npm, `yarn.lock` → yarn).
- **No services to start** — once application code is added, update this section with startup commands and any non-obvious caveats.
- **No lint/test/build commands** — these will need to be documented here once a build system is chosen.
