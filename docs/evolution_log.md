# Evolution Log

## Iteration 1 - 2026-03-09

### 本轮改进
- 首页新增“首次来站建议路径”三步模块（文化认同 → 谱系归属 → 连接与行动）。
- 优化首页四个主动作卡文案，统一为“先做什么，再进入下一步”的执行语言。
- 强化四大频道事实卡，补充“主线环节 + 下一步”信息，明确跨页面主线衔接。
- 优化“宗亲动态 / 公告”文案和 CTA，提升导流清晰度与回流价值表达。

### 结构变化
- 首页在 Hero 后新增一段路径引导模块，增强首次访问的信息架构清晰度。
- 未改动路由、导航结构和部署模型。

### 内容变化
- 首页行动文案更聚焦操作路径，减少抽象口号。
- 四大频道定位文案加入主线阶段标记，增强站内叙事一致性。
- 公告区改为“看更新 + 去下一步动作”的导向式表达。

### 验证
- `npm run typecheck` 通过。
- `npm run build` 通过（静态导出成功）。
- 本地环境端口受限，无法启动 dev server，因此本轮未产出桌面/移动端截图。

### 下一轮建议
- 首页增加“我现在该做什么”一键分流（首次访客 / 已有线索 / 想参与活动）。
- 文化传承页补一段“30 秒导读”摘要，提高首屏信息密度。
- 活动与礼品页增加“与家谱中心联动”的小型提示块，强化跨频道闭环。

## Iteration 2 - 2026-03-09

### 本轮改进
- 家谱中心页新增“提交前准备”模块，给出寻根与续谱的最小资料清单，降低首次提交反复补充成本。
- 文化传承页新增“30 秒导读”模块，把文化阅读明确衔接到寻根与活动两条行动路径。
- 祭祖活动页新增“参与路径”模块，按首次访客、报名中、活动后沉淀三种状态分流用户动作。
- 宣传礼品页新增“场景分流”模块，强化“先定场景再选礼品”的决策顺序与内容回流关系。

### 结构变化
- 在 `/genealogy/`、`/heritage/`、`/events/`、`/gifts/` 各新增一个中后段引导模块，用于明确下一步动作。
- 未改动顶层导航、路由结构、静态导出部署方式与域名策略。

### 内容变化
- 全站主线“文化认同 → 谱系归属 → 宗亲连接 → 参与行动 → 内容回流”在四个频道页的跳转文案中更显式。
- 减少抽象描述，增加可执行文案（准备什么、先去哪里、完成后回流到哪里）。

### 验证
- `npm run typecheck` 通过。
- `npm run build` 通过（静态导出成功）。
- 本轮未启动本地浏览器截图流程，暂未新增桌面端/移动端对比截图。

### 下一轮建议
- 首页新增“我现在该做什么”分流器（首次来访 / 已有家谱线索 / 只想参加活动）。
- 首页 Hero 区下沉一行“全站最新动态摘要”，减少首次进入的信息不确定性。
- 家谱中心页增加“常见退回原因”短列表，继续降低提交摩擦。

## Iteration 3 - 2026-03-09

### 本轮改进
- 首页 Hero 右侧新增“最新动态”摘要卡，直接展示 3 条关键更新并可一跳进入对应页面。
- 首页新增“我现在该做什么”分流模块（首次来站 / 已有线索 / 想参加活动 / 想做文化传播），降低首次访问决策成本。
- 首页 Hero 主叙事文案收紧，减少抽象说明，强化“主线固定 + 入口执行”的信息表达。
- 四个主动作卡统一补齐状态标签（优先入口/高频动作/连接入口/参与行动），提升视觉层级与入口辨识度。
- 优化寻根动作提示语，将“先读说明”明确为可执行动作（建议先读 5 分钟说明）。

### 结构变化
- 首页在 Hero 后新增状态分流区块，形成“首屏入口 → 状态分流 → 三步路径”的连续导流结构。
- 未改动顶层导航、对外路由、静态导出方式与部署模型。

### 内容变化
- 强化首页中的“最新动态”与“当下动作”联动，缩短“看到信息”到“进入行动”的路径。
- 首页文案从“门户定位描述”进一步转向“用户状态导向”，更贴近真实用户任务。

### 验证
- `npm run typecheck` 通过。
- `npm run build` 通过（静态导出成功）。
- 本轮未启动本地 dev server 截图流程，暂无新增桌面/移动端截图。

### 下一轮建议
- 首页“我现在该做什么”模块增加轻量指标提示（例如办理时长、所需材料数量）进一步提升可预期性。
- 家谱中心页增加“常见退回原因 TOP5 + 对应修正建议”短模块，继续减少提交摩擦。
- 活动页加入“本月关键时间点”微时间轴，强化参与节奏感。

## Iteration 4 - 2026-03-09

### 本轮改进
- 首页“我现在该做什么”四张分流卡新增轻量预期提示（耗时/准备量），把抽象路径转成可预期动作。
- 家谱中心页新增“常见退回原因 TOP5 + 修正建议”模块，直接针对提交反复补充场景做减摩擦。
- 祭祖活动页新增“本月关键时间点”微时间轴，压缩“看说明→报名→参与→回流”路径理解成本。

### 涉及页面
- `/`
- `/genealogy/`
- `/events/`

### 结构变化
- 首页分流模块保持原位置，仅增强卡片信息密度（低风险增量）。
- 家谱页与活动页各新增一个中后段说明模块，不改路由、不改导航。

### 内容变化
- 新增行动预期提示，减少用户“要准备什么/大约多久”的不确定感。
- 家谱页将常见失败点从隐性经验显式化，提升提交通过率预期。
- 活动页通过月内时间轴强化节奏感，避免用户只看活动标题不行动。

### 视觉变化
- 延续既有 `subtle-card` 与 `status-chip` 体系，未引入新样式分支。
- 模块层级遵循“说明 → 分流/时间轴 → CTA”统一节奏，视觉收敛保持稳定。

### 验证
- `npm run typecheck` 通过。
- `npm run build` 通过（静态导出成功）。
- 本轮未新增桌面/移动端截图（本地自动化运行环境未执行浏览器截图流）。

### 预览链接
- 暂无新的线上预览链接（本轮未触发部署）。
- 可本地基于最新构建产物验证改动。

### 是否建议合并 main
- 结论：`Needs one more iteration`。
- 原因：本轮为稳定增量优化，但缺少新预览链接与跨端实机截图，建议下一轮补齐后再评估合并。

### 下一轮建议
- 首页“我现在该做什么”卡片补充统一动作结果描述（例如“完成后进入哪一步”），继续强化主线闭环。
- 家谱中心页将 TOP5 退回原因与“提交前准备”建立一一映射，减少重复阅读。
- 活动页时间轴补充“活动后内容回流入口”显式链接，提升沉淀转化。

## Iteration 5 - 2026-03-09

### 本轮改进
- 首页“我现在该做什么”四张状态分流卡新增“完成后下一步去向”提示，明确跨频道闭环动作。
- 分流卡信息结构调整为“状态预期 → 当前说明 → 下一步去向 → 立即行动”，减少用户跳转犹豫。
- 新建 `docs/opportunity_pool.md`，把已完成与待执行优化项统一沉淀为机会池，便于小时级自动迭代持续选题。

### 涉及页面
- `/`

### 结构变化
- 首页仅增强既有分流卡信息密度，不新增路由、不改导航、不改部署模型。

### 内容变化
- 将“本步做完之后去哪里”显式化，降低首次访客在完成单步后的路径中断概率。
- 分流卡从“只给入口”升级为“给入口 + 给下一步”，强化“文化认同 → 谱系归属 → 连接行动 → 回流”主线。

### 视觉变化
- 延续既有 `status-chip`、`subtle-card`、强调色文本体系，仅新增一行轻量提示文案，不引入新样式分支。

### 验证
- `npm run typecheck` 已执行并通过。
- `npm run build` 已执行并通过（静态导出成功）。
- 本轮未执行浏览器截图流（自动化环境未启动本地 dev server）。

### 下一轮建议
- 家谱中心页将“提交前准备”与“TOP5 退回原因”做逐条映射，形成提交自检闭环。
- 活动页时间轴补充“活动后内容回流入口”显式链接，减少活动后沉淀流失。

## Iteration 6 - 2026-03-09

### 本轮改进
- 家谱中心页新增“提交前一一映射自检”模块，把准备动作与 TOP5 退回原因直接配对，降低用户在两块信息间来回切换成本。
- 祭祖活动页新增“活动后 72 小时回流清单”模块，给出 24h/48h/72h 三段式沉淀动作并补齐双入口 CTA（历届回顾 + 族人故事）。
- 机会池同步完成状态：OP-006、OP-007 标记为已完成，保持小时级迭代选题连续性。

### 涉及页面
- `/genealogy/`
- `/events/`

### 结构变化
- 两个频道页各新增一个中后段引导模块，均在现有 `soft-panel/subtle-card` 体系内增量完成。
- 未改动路由、导航、部署模型与依赖版本。

### 内容变化
- 家谱页把“准备什么”与“避免什么退回原因”直接绑定，用户可按映射顺序完成提交前自检。
- 活动页把“活动后回流”从概念描述升级为带时限的执行清单，降低活动结束后的内容断流风险。

### 视觉变化
- 继续复用既有卡片与分区层级，仅增强信息结构，不引入新视觉分支。
- CTA 位置保持统一：每个新增模块均遵循“说明 → 卡片信息 → 行动按钮”节奏。

### 验证
- `npm run typecheck` 通过。
- `npm run build` 通过（静态导出成功）。
- 本轮未执行浏览器截图流，暂无新增桌面/移动端截图。

### 预览链接
- 本轮未生成新的线上预览链接；当前仅保留本机临时预览记录。

### 是否建议合并 main
- 结论：`Needs one more iteration`。
- 原因：核心内容优化已连续落地，但仍缺少本轮对应的跨端截图证据与稳定线上 preview。

### 下一轮建议
- 首页状态分流卡补充统一“完成后的可见产出”短句，提升完成感与路径连续性。
- 文化传承页补一段“从故事回流到寻根动作”的桥接提示，进一步缩短频道跳转犹豫。

## Iteration 7 - 2026-03-09

### 本轮改进
- 首页“我现在该做什么”四张状态分流卡新增统一“可见产出”短句，明确每个入口完成后会得到什么结果。
- 分流卡信息节奏升级为“状态预期 → 当前说明 → 可见产出 → 下一步去向 → 行动入口”，补齐 OP-008。

### 涉及页面
- `/`

### 结构变化
- 首页仅在既有分流卡内增量补充一行信息，不新增模块、不改路由、不改导航。

### 内容变化
- 把“完成本步的结果”从隐含信息改为显式提示，降低用户完成动作后的不确定感与中断概率。
- 四张卡分别对应“入站路径确认 / 可提交资料包 / 可报名活动 / 可用礼品方案”四类可见结果，主线衔接更直接。

### 视觉变化
- 继续沿用 `subtle-card` 文本层级，仅增加一行低权重短句，不引入新样式分支。

### 验证
- `npm run typecheck` 通过。
- `npm run build` 通过（静态导出成功）。
- 本轮未执行浏览器截图流，暂无新增桌面/移动端截图。

### 预览链接
- 本轮未生成新的线上预览链接（环境端口监听受限，无法启动可访问本地预览）。

### 是否建议合并 main
- 结论：`Needs one more iteration`。
- 原因：内容链路已继续收敛，但仍缺少稳定 preview 链接与跨端截图证据。

### 下一轮建议
- 文化传承页增加“读完后立刻可执行的寻根桥接提示”，把文化阅读更短路径引向谱系动作。
- 首页 Hero 摘要增加“本周推荐先做一步”单条提示，进一步压缩首次访问决策成本。

## Iteration 8 - 2026-03-09

### 本轮改进
- 文化传承页新增“读完后立刻执行”桥接模块，把文化阅读直接收敛为“3 分钟寻根启动动作”。
- 模块内给出三步最小执行序列（写下可回访长辈、锁定县区/村镇范围、携带 3 条线索进入提交），补齐 OP-009。

### 涉及页面
- `/heritage/`

### 结构变化
- 在文化传承页末段新增一个 `soft-panel` 引导模块，不改路由、不改导航、不改部署方式。

### 内容变化
- 将“读完文化内容后做什么”从泛化 CTA 升级为可直接执行的最小动作，缩短“文化认同 → 谱系归属”的跳转犹豫。
- 三步文案与家谱页既有退回经验保持一致，避免跨页信息冲突。

### 视觉变化
- 完整复用现有 `soft-panel + subtle-card` 体系，仅新增内容层，不引入新视觉分支。

### 验证
- `npm run typecheck` 通过。
- `npm run build` 通过（清理 `.next` 后重建成功）。
- 本地预览可访问：`http://127.0.0.1:4173`（`curl -I` 返回 `HTTP/1.1 200 OK`）。

### 预览链接
- 本轮最新预览：`http://127.0.0.1:4173`
- 重点查看：`/heritage/` 新增“读完后立刻执行”模块与三步桥接卡片。

### 是否建议合并 main
- 结论：`Needs one more iteration`。
- 原因：虽然已有可访问预览，但尚缺 `/`、`/genealogy/`、`/events/` 的跨端截图证据与终审记录。

### 下一轮建议
- 执行 OP-010：首页 Hero 增加“本周推荐先做一步”单条提示，继续压缩首次访客决策成本。
- 补齐首页、家谱页、活动页的桌面端/移动端截图，作为合并前视觉证据包。

## Iteration 8 - 2026-03-09

### 本轮改进
- 首页首屏右侧新增“本周推荐先做一步”提示卡，给首次来访用户一条默认起步动作（先读文化传承，再进入寻根）。
- 推荐卡直接放在“最新动态”之后、“四个主动作”之前，减少用户进入首页后的初始选择成本。
- 机会池同步更新：OP-010 标记已完成，并新增 OP-011（补充推荐理由文案）。

### 涉及页面
- `/`

### 结构变化
- 首页仅新增一个轻量提示卡，不新增路由、不改导航、不改部署模型。

### 内容变化
- 把“建议做什么”从分散提示改为首屏单点推荐，缩短首次访客从阅读到动作的路径。
- 推荐文案延续“文化认同 → 谱系归属”主线，避免用户直接进入办事页而缺少认知上下文。

### 视觉变化
- 复用现有圆角卡片体系，仅做边框强调，不引入新组件或新样式分支。
- 模块层级更清晰：最新动态（发生了什么）→ 本周推荐（先做什么）→ 主动作入口（去哪里做）。

### 验证
- `npm run typecheck` 通过。
- `npm run build` 通过（静态导出成功）。
- 本地检测到 `3000` 端口服务在运行；已尝试通过 Playwright MCP 与 Playwright CLI 抓取桌面/移动截图，但均因沙箱浏览器进程权限限制失败。

### 下一轮建议
- 为“本周推荐先做一步”补充一句“为何本周优先”的解释文案，提升推荐可信度与接受度。

## Iteration 9 - 2026-03-09

### 本轮改进
- 首页“本周推荐先做一步”提示卡新增“为何本周优先”一句理由文案，补齐推荐依据，减少用户对推荐主观性的疑问。
- 机会池状态校准：OP-011 标记为已完成；同时将已在迭代 2 落地的 OP-009 补记为已完成，避免自动化重复选题。
- 新增 OP-012（推荐动态化触发条件）作为后续候选，保持小步迭代连续性。

### 涉及页面
- `/`

### 结构变化
- 首页仅在既有推荐卡内新增一行说明文案，不新增模块、不改路由、不改导航。

### 内容变化
- “推荐动作 + 推荐理由”配对呈现，用户可快速理解为什么该先做这一步。
- 推荐逻辑继续围绕“文化认同 → 谱系归属”主线，降低首次访客的路径断点。

### 视觉变化
- 复用现有文字层级，仅补充一行低权重说明文本，不引入新样式分支。

### 验证
- `npm run typecheck` 通过。
- `npm run build` 通过（静态导出成功）。
- 本轮未执行浏览器截图流（自动化环境未调用可用截图通道）。

### 下一轮建议
- 探索将“本周推荐”由静态文案升级为按活动节奏切换的动态推荐（例如活动周优先活动、寻根周优先家谱）。

## Iteration 10 - 2026-03-10

### 本轮改进
- 首页“本周推荐先做一步”从单段提示升级为三步执行清单，并补充“完成后去寻根”二跳入口，减少用户看完推荐后停留不动。
- 首页“四个主动作”卡片改为差异化 CTA（进入寻根流程 / 查看续谱说明 / 查看近期活动 / 按场景选礼品），替换统一“进入入口”的弱语义按钮。
- 首页“我现在该做什么”模块新增默认起步提示（优先选择“我是第一次来站”），继续压缩首次访客决策成本。
- 机会池状态修正：OP-010 标记为已完成，新增并完成 OP-012/OP-013/OP-014；OP-011 保持待执行并注明 Playwright 环境冲突。

### 涉及页面
- `/`

### 结构变化
- 首页仅在既有卡片模块内增量增强，不新增路由、不改导航、不改部署模型。

### 内容变化
- 推荐模块由“告诉你做什么”升级为“告诉你怎么做 + 做完去哪”，闭环更完整。
- 主动作入口语义更具体，减少按钮文案重复导致的选择模糊。
- 状态分流增加默认起步建议，首次访问者更容易在 10 秒内确定入口。

### 视觉变化
- 完整复用 `soft-panel / subtle-card / status-chip` 体系，仅增加一层轻量清单容器与提示文字。
- 不新增视觉分支，保持既有层级与留白节奏。

### 验证
- `npm ci` 成功（恢复本地依赖）。
- `npm run typecheck` 通过。
- `npm run build` 通过（静态导出成功）。
- 本地预览可访问：`http://127.0.0.1:4173`（`curl -I` 返回 `HTTP/1.0 200 OK`）。
- 已尝试使用 Playwright MCP 生成跨端截图证据包，但浏览器进程被“正在现有会话中打开”冲突中断，本轮未能产出截图。

### 预览链接
- 本轮最新预览：`http://127.0.0.1:4173`
- 重点查看：首页首屏右侧“本周推荐先做一步”卡片、四个主动作卡 CTA、状态分流区默认起步提示。

### 是否建议合并 main
- 结论：`Needs one more iteration`。
- 原因：功能与构建状态稳定，但合并前仍缺首页/家谱/活动/文化四页桌面+移动截图证据包。

### 下一轮建议
- 优先处理 OP-011：排查 Playwright 会话冲突并补齐四页跨端截图证据。
- 若截图恢复，进入一次“预览走查 + merge_readiness 收口”迭代，评估是否切换为 `Ready for main`。

## Iteration 11 - 2026-03-10

### 本轮改进
- 活动页新增“报名前 2 分钟自检”模块（3 步最小准备动作 + 双 CTA），补齐“看活动后仍迟疑不报名”的断点。
- 补齐 OP-011：已生成 `/`、`/genealogy/`、`/events/`、`/heritage/` 四页的桌面端与移动端截图证据包。
- 截图通道由受阻的 Playwright MCP 切换为 `npx playwright screenshot`，解除跨轮证据产出阻塞。

### 涉及页面
- `/events/`

### 结构变化
- 活动页在“本月关键时间点”和“72 小时回流清单”之间新增一个中段引导模块。
- 未改动导航、路由、部署模型与依赖版本。

### 内容变化
- 将报名前准备动作显式化为三步（确定场次、确认同行与到场方式、预留可联系号码）。
- 把“活动信息阅读”收敛到“可执行报名准备”，提升从浏览到行动的连续性。

### 视觉变化
- 新增模块完整复用现有 `soft-panel + subtle-card` 体系，不引入新样式分支。
- 保持“说明 → 卡片 → CTA”既有页面节奏，视觉风险低。

### 验证
- `npm run typecheck` 通过。
- `npm run build` 通过（静态导出成功）。
- 本地预览可访问：`http://127.0.0.1:4173`（`curl -I` 返回 `HTTP/1.0 200 OK`）。
- 跨端截图证据已生成于 `output/playwright/iteration-11/`（8 张）。

### 预览链接
- 本轮最新预览：`http://127.0.0.1:4173`
- 重点查看：`/events/` 新增“报名前 2 分钟自检”模块。

### 是否建议合并 main
- 结论：`Preview only, not ready for main`。
- 原因：虽然构建和截图证据齐备，但当前仍缺本轮分支对应的稳定线上 preview 与人工终审结论。

### 下一轮建议
- 基于 `output/playwright/iteration-11/` 进行一次人工视觉终审，确认是否需要微调文案密度。
- 若终审通过，生成稳定 preview 链接并切换 merge_readiness 到 `Ready for main`。

## Iteration 12 - 2026-03-10

### 本轮改进
- 首页“本周推荐先做一步”新增“已有线索，直接去寻根”跳过入口，降低高意图用户被固定推荐路径拖慢的概率。
- 首页“我现在该做什么”分流区新增“动作后回流”三卡，把寻根、活动、礼品动作后的内容沉淀入口前置。
- 产出首页跨端截图证据（desktop/mobile），用于本轮首页改动终审。

### 涉及页面
- `/`

### 结构变化
- 首页仅在既有推荐卡与状态分流区增量补信息，不新增路由、不改导航、不改部署模型。

### 内容变化
- 推荐卡新增“路径不适用时的快速跳转”，减少已有线索用户路径阻塞。
- 首页首次显式给出“动作后回流”示例，将“内容回流”从末段概念前移为分流后的即时动作。

### 视觉变化
- 完整复用 `soft-panel / subtle-card` 体系，不引入新组件与新样式分支。
- 新增三卡保持与既有分流卡同节奏，视觉层级连续。

### 验证
- `npm run typecheck` 通过。
- `npm run build` 通过（静态导出成功）。
- 本地预览可访问：`http://127.0.0.1:4173`（`curl -I` 返回 `HTTP/1.0 200 OK`）。
- 首页截图证据：`output/playwright/iteration-12/home-desktop.png`、`output/playwright/iteration-12/home-mobile.png`。

### 预览链接
- 本轮最新预览：`http://127.0.0.1:4173`
- 重点查看：首页“本周推荐”卡片新增跳过入口、状态分流区新增“动作后回流”三卡。

### 是否建议合并 main
- 结论：`Preview only, not ready for main`。
- 原因：当前仅有本地临时预览，仍缺本轮稳定线上 preview 与最终人工终审结论。

### 下一轮建议
- 执行 OP-019：在“动作后回流”卡中补充“1 句事实 + 1 句经验”回流模板，继续降低回流表达门槛。
- 若首页视觉终审通过，直接进入合并发布闭环。

### Iteration 12 补充修正（用户导向文案） - 2026-03-10

- 按“面向用户导向、避免自言自语”反馈，重写首页关键文案：Hero 说明、推荐理由、分流建议、文化/家谱/礼品区标题与说明。
- 文案统一转为“你该做什么、为什么这样做、做完去哪里”表达，弱化站点架构自述。
- 验证结果保持通过：`npm run typecheck`、`npm run build`。
- 已刷新首页截图证据：`output/playwright/iteration-12/home-desktop.png`、`output/playwright/iteration-12/home-mobile.png`。

## Iteration 13 - 2026-03-10

### 本轮改进
- 删除首页 Hero 内的“公开浏览，登录办事”辅助文案。
- 删除右侧侧栏底部“给你的首页建议”整卡。
- 删除“动作后回流”整块与“首次来站建议路径”整段，保留更高价值的状态分流与频道入口。

### 涉及页面
- `/`

### 结构变化
- 首页整体长度收缩，首屏以下减少两整段重复解释块。
- 未改动路由、导航与部署模型。

### 内容变化
- 删除重复表达“先做什么/先认同再归属”的二次说明，避免首页连续解释同一判断。
- 保留最关键入口：Hero、最新动态、本周推荐、主动作、状态分流、频道预览。

### 验证
- `npm run typecheck` 通过。
- `npm run build` 通过。
- 本地预览可访问：`http://127.0.0.1:4173`。
- 已刷新首页截图证据：`output/playwright/iteration-13/home-desktop.png`、`output/playwright/iteration-13/home-mobile.png`。

### 是否建议合并 main
- 结论：已按用户要求直接发布生产。
